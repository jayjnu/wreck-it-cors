import { Typography } from '@mui/material';
import { Box } from '@mui/system';
import { FC } from 'react';

export const SameOriginValidator: FC = () => {
  return (
    <Box>
      <Typography>Same Origin Validator</Typography>
    </Box>
  );
};
